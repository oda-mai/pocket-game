<div class="tweet-detail-container">
  <h1 class="main-title">ぽけっとあそび</h1>
  <h3 class="sub-title">-投稿詳細-</h3>

  <div class="tweet-card">
    <p><strong>タイトル：</strong><%= @tweet.name %></p>
    <p><strong>人数：</strong><%= @tweet.number %></p>
    <p><strong>内容：</strong><%= @tweet.content %></p>
    <p><strong>投稿日：</strong><%= @tweet.created_at.strftime("%Y年%m月%d日 %H:%M") %></p>
  </div>

  <div class="tweet-actions">
    <%= link_to "✏️ 編集", edit_tweet_path(@tweet.id), class: "action-button" %>
    <%= button_to "🗑️ 削除", tweet_path(@tweet.id), method: :delete, data: { confirm: "本当に削除しますか？" }, class: "action-button danger" %>
  </div>

  <div class="back-link1">
    <%= link_to "投稿一覧に戻る", tweets_path, class: "back-button" %>
  </div>
</div>